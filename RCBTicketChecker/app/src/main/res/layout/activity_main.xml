<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1a1a1a"
    android:padding="24dp"
    android:gravity="center">

    <!-- Logo / Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🏏 RCB Ticket Checker"
        android:textSize="24sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="RCB vs GT — Apr 24, 2026"
        android:textSize="14sp"
        android:textColor="#CC0000"
        android:layout_marginBottom="40dp" />

    <!-- Status Card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#2a2a2a"
        android:padding="20dp"
        android:layout_marginBottom="32dp">

        <!-- Status Indicator -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="8dp">

            <View
                android:id="@+id/statusIndicator"
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:background="@android:drawable/presence_online"
                android:layout_marginEnd="10dp" />

            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="NOT MONITORING"
                android:textSize="18sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvSubStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tap Start to begin"
            android:textSize="13sp"
            android:textColor="#AAAAAA" />
    </LinearLayout>

    <!-- Start / Stop Button -->
    <Button
        android:id="@+id/btnStartStop"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="START CHECKER"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:backgroundTint="@color/green"
        android:layout_marginBottom="12dp" />

    <!-- Stop Alarm Button -->
    <Button
        android:id="@+id/btnStopAlarm"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="STOP ALARM"
        android:textSize="14sp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#FF6600"
        android:layout_marginBottom="12dp" />

    <!-- Buy Tickets Button -->
    <Button
        android:id="@+id/btnBuyTickets"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="BUY TICKETS NOW"
        android:textSize="14sp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#CC0000"
        android:layout_marginBottom="32dp" />

    <!-- Info -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Alerts you when:\n• GT match is added\n• Any new match added\n• Ticket status changes\n• Every 30 min status report\n\nAlarm repeats every 5 seconds\nuntil you tap STOP ALARM"
        android:textSize="12sp"
        android:textColor="#888888"
        android:gravity="center" />

</LinearLayout>
